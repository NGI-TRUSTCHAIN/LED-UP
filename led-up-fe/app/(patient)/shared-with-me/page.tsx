import SharedWithMePage from '@/features/data-registry/components/shared-with-me';

const Page = () => {
  // This will be only for consumer or provider role
  return <SharedWithMePage />;
};

export default SharedWithMePage;
